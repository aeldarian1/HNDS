'use client';

// Client-side providers for global features
// Currently empty - previously contained SmoothScroll which was unused
// Keeping this file for potential future client providers (e.g., analytics, error boundaries)

export function LayoutClientProviders() {
  return null;
}
