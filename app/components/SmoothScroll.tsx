'use client';

export default function SmoothScroll() {
  // Lenis was causing choppiness - using browser native smooth scroll instead
  // Applied in globals.css with scroll-behavior: smooth
  return null;
}
